# Claude Code Instructions — FxFixGateway.V2-DEV

## CRITICAL: Git Policy
- **NEVER** run `git commit`, `git push`, `git add`, or any git write command.
- **NEVER** edit, create, or delete files in the repository.
- **ONLY** show code as text output. The user implements all changes and pushes to origin/master.