<Window x:Class="FxFixGateway.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:FxFixGateway.UI.ViewModels"
        xmlns:views="clr-namespace:FxFixGateway.UI.Views"
        mc:Ignorable="d"
        Title="Fx Fix Gateway"
        Height="800" 
        Width="1400"
        MinHeight="600"
        MinWidth="1000"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        Loaded="Window_Loaded">
 

    <Window.Resources>
        <!-- Styles här senare -->
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <Menu Grid.Row="0" Background="{DynamicResource MaterialDesignPaper}">
            <MenuItem Header="_File">
                <MenuItem Header="_Refresh All" Command="{Binding RefreshAllCommand}"/>
                <Separator/>
                <MenuItem Header="E_xit" Command="{Binding ExitCommand}"/>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="_About"/>
            </MenuItem>
        </Menu>

        <!-- Main Content Area -->
        <Grid Grid.Row="1" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" MinWidth="300"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="3*" MinWidth="400"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Session List -->
            <Border Grid.Column="0" 
                    Background="{DynamicResource MaterialDesignCardBackground}"
                    CornerRadius="4"
                    Margin="0,0,4,0">
                <views:SessionListView DataContext="{Binding SessionList}"/>
            </Border>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" 
                         Width="6" 
                         HorizontalAlignment="Center" 
                         VerticalAlignment="Stretch"
                         Background="Transparent"
                         ShowsPreview="True"/>

            <!-- Right Panel: Session Detail -->
            <Border Grid.Column="2" 
                    Background="{DynamicResource MaterialDesignCardBackground}"
                    CornerRadius="4"
                    Margin="4,0,0,0">
                <views:SessionDetailView DataContext="{Binding SessionDetail}"/>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2" Background="{DynamicResource MaterialDesignPaper}">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusBarText}" 
                          Margin="8,0"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="{Binding SessionList.Sessions.Count, StringFormat='Sessions: {0}'}" 
                          Margin="8,0"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="{Binding SessionList.RunningSessionsCount, StringFormat='Running: {0}'}" 
                          Margin="8,0"
                          Foreground="#4CAF50"/>
            </StatusBarItem>
        </StatusBar>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="3" 
              Background="#AA000000" 
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" 
                       VerticalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                            Value="0"
                            IsIndeterminate="True"
                            Width="50"
                            Height="50"/>
                <TextBlock Text="Loading..." 
                          Foreground="White"
                          FontSize="16"
                          Margin="0,16,0,0"
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
